{"resource-aws-lightsail-lb-stickiness-policy": "<h1 id=\"resource-aws_lightsail_lb_stickiness_policy\">Resource: aws_lightsail_lb_stickiness_policy</h1><p>Configures Session Stickiness for a Lightsail Load Balancer.</p>", "example-usage": "<h2 id=\"example-usage\">Example Usage</h2><p>terraform<br />resource \"aws_lightsail_lb\" \"test\" {<br />  name              = \"test-load-balancer\"<br />  health_check_path = \"/\"<br />  instance_port     = \"80\"<br />  tags = {<br />    foo = \"bar\"<br />  }<br />}</p><br /><p>resource \"aws_lightsail_lb_stickiness_policy\" \"test\" {<br />  lb_name         = aws_lightsail_lb.test.name<br />  cookie_duration = 900<br />  enabled         = true<br />}</p><br />", "argument-reference": "<h2 id=\"argument-reference\">Argument Reference</h2><p>This resource supports the following arguments:</p><ul><li><code>lb_name</code> - (Required) The name of the load balancer to which you want to enable session stickiness.</li><li><code>cookie_duration</code> - (Required) The cookie duration in seconds. This determines the length of the session stickiness.</li><li><code>enabled</code> - (Required) - The Session Stickiness state of the load balancer. <code>true</code> to activate session stickiness or <code>false</code> to deactivate session stickiness.</li></ul>", "attribute-reference": "<h2 id=\"attribute-reference\">Attribute Reference</h2><p>This resource exports the following attributes in addition to the arguments above:</p><ul><li><code>id</code> - The name used for this load balancer (matches <code>lb_name</code>).</li></ul>", "import": "<h2 id=\"import\">Import</h2><p>In Terraform v1.5.0 and later, use an <a href=\"https://developer.hashicorp.com/terraform/language/import\"><code>import</code> block</a> to import <code>aws_lightsail_lb_stickiness_policy</code> using the <code>lb_name</code> attribute. For example:</p><p>terraform<br />import {<br />  to = aws_lightsail_lb_stickiness_policy.test<br />  id = \"example-load-balancer\"<br />}</p><p>Using <code>terraform import</code>, import <code>aws_lightsail_lb_stickiness_policy</code> using the <code>lb_name</code> attribute. For example:</p><p>console<br />% terraform import aws_lightsail_lb_stickiness_policy.test example-load-balancer</p>", "description": "<h1 id=\"resource-aws_lightsail_lb_stickiness_policy\">Resource: aws_lightsail_lb_stickiness_policy</h1><p>Configures Session Stickiness for a Lightsail Load Balancer.</p>"}